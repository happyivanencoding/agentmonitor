const test=require('node:test');const assert=require('node:assert/strict');const p=require('./load-extractor.cjs');
const cid='12345678-1234-1234-1234-123456789abc';
test('direct AgentDock recipient activity keeps safe project hint',()=>{const invocation={id:'direct-1',author:{role:'assistant'},recipient:'AgentDock.exec_command',content:{text:JSON.stringify({workdir:'C:\\dev\\agent-monitor',cmd:'PRIVATE'})}};const a=p.invocationActivity(invocation,{conversationId:cid,title:'x'},{id:'u-direct',text:'构建'},'direct-1');assert.equal(a.toolName,'AgentDock.exec_command');assert.match(a.projectHint,/agent-monitor/);assert(!a.argsSummary.includes('PRIVATE'));});
test('ordinary read_file JSON containing ACP-like fields never becomes ACP binding',()=>{const m={author:{role:'tool',name:'AgentDock.read_file'},content:{parts:[JSON.stringify({id:'acps_1234567890abcdef12345678',remote_session_id:'00000000-0000-4000-8000-000000000004',agent:'codex'})]}};assert.deepEqual(p.extractTool(m,{conversationId:cid,title:'x'}),[]);});
